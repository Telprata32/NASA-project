import 'package:flutter/material.dart';

class Negeri extends StatefulWidget {
  const Negeri({Key? key}) : super(key: key);

  @override
  _NegeriState createState() => _NegeriState();
}

class _NegeriState extends State<Negeri> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Column(
            children: [

            ],
          ),
        ),
      ),
    );
  }
}
